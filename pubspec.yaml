name: appflowy_editor_plugins
description: AppFlowy Plugins
version: 0.0.1
homepage: https://github.com/LucasXu0/appflowy_editor_plugins

environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  appflowy_editor: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
